# Next backend task

STATUS: EMPTY

The orchestrator must replace this template with a complete, scoped task before starting either one-shot backend task in VS Code. The launcher intentionally rejects this empty template.

```text
Objective: [TODO: one outcome]
Tier: [TODO: light or heavy]
Owned files: [TODO: exact paths]
Read-only dependencies: [TODO: contract/source/docs]
Contract: [TODO: inputs, outputs, errors, persistence, side effects]
Acceptance: [TODO: observable success criteria]
Checks: [TODO: only relevant commands]
Out of scope: [TODO: nearby work not assigned]
```

For a Light review task, provide the Heavy checkpoint's frozen paths, the exact diff, the relevant contract, and whether Light may edit each path. Never include credentials, `.env.local`, customer data, or unrelated repository context.
